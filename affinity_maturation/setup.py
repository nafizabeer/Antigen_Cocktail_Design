#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='cocktail_optimization',
    version='0.1',
    description='',
    install_requires=[],
    packages=find_packages(),
)